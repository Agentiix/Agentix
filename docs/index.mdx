---
title: Agentix
description: Typed Python namespaces for sandbox-based agent workflows.
---

Agentix lets you compose agents, datasets, and primitive tools into a
sandbox and call them from your trainer or harness as typed Python:

```python
from agentix import RuntimeClient
from agentix import bash, claude_code, swebench

async with RuntimeClient(sandbox_url) as c:
    cc = await c.remote(
        claude_code.run, instruction=problem, workdir="/testbed",
    )
    s = await c.remote(swebench.score, instance=inst, patch=cc.patch)
```

Every extension is a normal pip-installable distribution. No custom
config file, no decorator at import time, no per-framework registry
call — `pip install` the wheel and the framework discovers it via
Python entry points.

## What's here

<CardGroup cols={2}>
  <Card title="Quick start" icon="rocket" href="/quickstart">
    Install, call a namespace, write your own.
  </Card>
  <Card title="Namespace" icon="cube" href="/namespace">
    A Python package whose top-level async functions are remote-callable.
  </Card>
  <Card title="Deployment" icon="server" href="/deployment">
    Backend that provisions sandboxes — `local` / `daytona` / `e2b`.
  </Card>
  <Card title="Architecture" icon="sitemap" href="/reference/architecture">
    Discovery, dispatch, and runtime layout.
  </Card>
  <Card title="Namespace protocol" icon="diagram-project" href="/reference/namespace-protocol">
    Wire-format contract between caller and sandbox.
  </Card>
  <Card title="CLI" icon="terminal" href="/reference/cli">
    `agentix build / deploy / check`.
  </Card>
</CardGroup>
